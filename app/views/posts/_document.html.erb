<div class="docPost">
  
    <div class="docIcon">
      <% # TODO: this should open in new tab %>
      <%= link_to raw("<i class='icon-file'></i>"), post.attachment.url %>
    </div>

    <div class="docTitle">
      <%= post.title %>
    </div>

    <div class="docInfo">
      <%= post.created_at.strftime("%d %b %Y") %>
    </div>

  <div class="postControls">
      <%= link_to raw("<i class='icon-edit'></i>"), edit_post_path(post) %>
      <%= link_to raw("<i class='icon-trash'></i>"), post, method: :delete,
      confirm: "Are you sure you want to delete this document?" %>
  </div>

  </a>

<!--
  <% if post.attachment.present? %>
  <div class="docLink">
    <%= link_to post.attachment.file.filename, post.attachment.url %>
  </div>
  <% end %>
-->
  
  <div class="clearfix"></div>
</div>
